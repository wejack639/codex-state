# 面板宠物目录

直接用一个完整的 Codex 宠物目录覆盖这里的文件：

- 必须包含 `pet.json`。
- 图集必须是 `pet.json` 的 `spritesheetPath` 指向的同目录 `.png` 或 `.webp` 文件。
- 支持 `spriteVersionNumber` 1 和 2。
- 文件替换完成后，正在运行的面板会在 2 秒左右自动加载。

这里始终只保留当前皮肤，不维护皮肤列表。
