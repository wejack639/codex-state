# CODEXSTATE-APPEARANCE-001 — 实施与验收记录

日期：2026-09-10。结论：主题功能已实现并在现有原生窗口运行；图标及最终重新打包安装尚未完成，不能将整个合同标为通过。

## 已实现

- 前后端共用七套主题目录，面板和浏览器均有调色盘入口。
- 本机配置保存 `theme`，旧配置补为午夜深色，保留关注、重点关注和已查看时间；主题通过原有 SSE 同步。
- 点击色卡立即应用，保存失败显示错误并撤销乐观配色；保存期间避免重复请求，支持点击外部或 Esc 关闭选择器。
- 全套背景、卡片、按钮、输入框和悬停样式使用主题变量；重点关注改为金星、淡主题色底和细边，红色未读可同时显示。
- 未修改原生尺寸、窗口层级、会话打开路由、宠物图集、动画、拖动及收回逻辑。

## 自动验证

| 执行项 | 实际结果 |
| --- | --- |
| `npm test` | 29 项通过、0 失败；包含 27 项状态/接口/宠物测试、生产构建及 2 项 SSR 回归。 |
| `npm run lint` | 通过。 |
| `git diff --check` | 通过。 |
| `npx tsc --noEmit --incremental false` | 未通过：既有 `worker/index.ts` 第 6、7 行缺少 `Fetcher`、`D1Database` 类型；未修改该文件或把本项计为通过。 |

新增 `tests/theme-api.test.mjs` 使用独立临时目录和临时 SQLite 库，不修改用户 Codex 数据，覆盖：

1. 旧格式配置迁移，保留重点关注和红色未查看信息。
2. 七个合法主题的响应与配置文件一致。
3. 主题变更经 SSE 广播。
4. 添加、标星、取消标星、打开和移除后主题不被覆盖。
5. 拒绝非法主题名称及非本机来源，原有配置不变。
6. 整理配置和重新加载服务后主题保留。

## 真实原生窗口记录

使用 macOS `com.domino.codex-state` 应用及其 WKWebView，地址 `localhost:3000/?pet=1`。本轮对原生窗口进行了连续操作并查看实际截图与辅助功能树，不以源码选择器或接口成功替代画面。截图保留在本轮工具记录中，未另行导出到仓库。

| 主题 | 关注列表、卡片与按钮 | 名称编辑框 | 添加搜索框 | 四类状态灯及金星/红色共存 |
| --- | --- | --- | --- | --- |
| 冰川蓝 | 已核对 | 已核对 | 已核对 | 已核对 |
| 樱花粉 | 已核对 | 已核对 | 已核对 | 已核对 |
| 薄荷绿 | 已核对 | 已核对 | 已核对 | 已核对 |
| 雾霭紫 | 已核对 | 已核对 | 已核对 | 已核对 |
| 蜜桃橙 | 已核对 | 已核对 | 已核对 | 已核对 |
| 云朵白 | 已核对 | 已核对 | 已核对 | 已核对 |
| 午夜深色 | 已核对 | 已核对 | 已核对 | 已核对 |

- 选择器位于“收回 / 添加”左侧，内部四列色卡，包含全部七项及当前勾选；连续切换、切回时可在当前面板完成，无独立设置窗口。
- 运行中的会话对应绿灯；历史已查看完成会话对应灰灯。为覆盖未知黄灯，暂时添加现有的“前端展示和操作方案-流程优化 (2)-恢复副本”，与只读快照中的 `unknown` 一致。
- 实际会话“海鸿采购计划里找不到此任务”在验证期间产生未读完成结果；临时标星后逐套检查金星与红色提示共存。验收后恢复该会话原先的非重点关注状态。
- 编辑框只打开和取消，没有改写任何真实会话名称。搜索词“恢复副本”已清除；上述临时添加的未知会话已从面板移除，没有删除真实会话。
- 点击 `Add Codex chat状态监控面板` 的“打开”后，面板立即缩为透明小人，VS Code Codex 侧栏显示该准确会话。没有将仅打开工作目录作为成功证据。
- 实际拖动位置发生变化，手动收回可恢复透明小人；窗口保留双列列表、既有 `112 × 128` 收起尺寸，未添加白底框。
- 当前原生窗口读取项目中的新版页面，并已重启本机服务加载主题接口。它还不是最终新图标安装包的验收；最终安装后需再次核对持久化和系统图标。
- 用户在验证期间仍在操作其他会话和主题，未用旧配置整体覆盖当前配置。

## 未完成项与原因

- 应用图标：内置图像工具三次生成/定向修正的候选均为 RGB，`sips -g hasAlpha` 返回 `no`，图标圆角外是画进图片的棋盘格或灰纹理，不是真透明。没有将这些候选写入 `public` 或替换正式图标。
- 已请求用户批准用本地程序仅裁掉外围棋盘格、生成圆角透明边缘，人物内容不变。批准前不擅自换用图像工具之外的编辑方式。
- 批准后仍需交付高清 PNG 与多尺寸 `.icns`，更新打包来源，重新打包、安装、重启，并通过 Finder 核对应用和桌面快捷方式均实际显示新图标。
- 最终安装后的主题重启恢复、逐套透明边缘及悬停/移入移出完整画面需一并收尾。当前阶段不将 AC-008、AC-009 或合同整体标为通过。

上述实施验收阶段未 commit、未 push，也未部署到云端。随后用户通过 `gryy-push` 授权提交当前变更；提交记录不代表未完成的图标及安装验收已经通过。
